package tests.modelling_tests;

/**
 * The KellimniAccountStates enum contains the states that an account can be in
 */

public enum KellimniAccountStates {
	INVALID_LOGIN_LOCKED,
	PARENTAL_LOCKED,
	UNLOCKED,
}
